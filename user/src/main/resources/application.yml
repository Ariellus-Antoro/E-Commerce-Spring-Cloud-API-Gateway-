server:
  port: 8202

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/db_ecommerce_transaction?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: SkripsiTokenRotationBlacklistRedisAmanBanget2026